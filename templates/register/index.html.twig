{% extends 'base.html.twig'%}
 
{% block content %}

  <h1>Inscription</h1>
  {% if notification %}
  <div class="alert alert-info">{{notification}}</div>
  <hr>
  {% endif %} 
  <div class="container mt-sm-n1">
    {{form(form)}}
  </div>


{% endblock  %}