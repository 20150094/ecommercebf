<div class="product-item  text-center">
        
                    <a href="{{path('product',{'slug':product.slug})}}"><img src="/Images/{{product.illustration}}" alt="{{product.name}}" class="img-fluid"></a>
                    <h5>{{product.name}}</h5>
                    <span class="product-subtitle">{{product.subtitle}}</span>
                    <span class="product-price mt-0 bg-warning"> {{(product.prix/100) |number_format(2,',','.')}} € |  {{((product.prix/100)*655.51)  |number_format(2,',','.')}} FCFA</span>
                
</div>